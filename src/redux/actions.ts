export const SET_USER = (user) => ({
  type: "SET_USER",
  payload: user,
})

export const LOGOUT = () => ({
  type: "LOGOUT",
})

export const SET_CART_COUNT = (count) => ({
  type: "SET_CART_COUNT",
  payload: count,
})